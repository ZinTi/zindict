### 重要说明
- 使用 Rust 重构项目  
  可执行文件名称已由 `dict` 改为 `zindict`。若需要使用 `dict` 作为命令，可自行创建命令行别名（unix-like 使用 alias 命令，windows 可新建 `dict.bat`脚本）。

### 新增功能
- 新增配置功能，可自定义命令行颜色和数据库路径等

### 功能改进
- 无

### 问题修复
- 无

### 依赖更新
- 略
